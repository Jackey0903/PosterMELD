# User-requested Templates Are Not Silently Replaced

Paper2Poster will respect an explicitly requested template even when Template Compatibility is weak, surfacing warnings and relying on repair or quality gates to decide success. Template Fallback is allowed by default only for automatically selected variants, or when the user explicitly enables fallback behavior, so generated posters remain reproducible and user intent is not hidden by selection heuristics.

